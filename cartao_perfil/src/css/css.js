import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  viewPrinc: {
    borderWidth: '3px',
    borderColor: 'black',
    marginTop: '20px',
    width: '70%',
    background: 'grey',
    alignItems: 'center',
    alignSelf: 'center',
  },
});

export {styles}